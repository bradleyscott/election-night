# dockerignore patterns without a leading slash are anchored to the
# context root, so use **/ for anything that also appears nested under
# packages/* (node_modules, .cache, .data, dist)
**/node_modules
**/.cache
**/.data
**/dist
.git
.gitignore
.env
.env.local
.vscode
*.md
.DS_Store
coverage
